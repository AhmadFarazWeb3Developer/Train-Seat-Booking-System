<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User's Booked Train</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 text-gray-900">
    <div class="container mx-auto mt-10 px-4">
      <h1 class="text-3xl font-bold text-center text-indigo-600 mb-6">
        Booked Train Details
      </h1>

      <% if (user && bookedTrain) { %>
      <table
        class="min-w-full bg-white border border-gray-300 shadow-md rounded-lg mb-6"
      >
        <thead>
          <tr class="bg-indigo-500 text-white">
            <th class="py-2 px-4 border-b">User Information</th>
            <th class="py-2 px-4 border-b">Details</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td class="py-2 px-4 border-b text-right font-semibold">Name:</td>
            <td class="py-2 px-4 border-b"><%= user.customer_name %></td>
          </tr>
          <tr>
            <td class="py-2 px-4 border-b text-right font-semibold">Email:</td>
            <td class="py-2 px-4 border-b"><%= user.customer_email %></td>
          </tr>
          <tr>
            <td class="py-2 px-4 border-b text-right font-semibold">Phone:</td>
            <td class="py-2 px-4 border-b"><%= user.customer_phone %></td>
          </tr>
        </tbody>
      </table>

      <% bookedTrain.forEach((train) => { %>
      <form
        class="bg-white shadow-md rounded-lg p-6 mb-6"
        action="/user/booking/cancle/<%= train._id%>"
        method="post"
      >
        <h3 class="text-xl font-semibold text-indigo-500 mb-4">
          Train Details
        </h3>
        <table class="min-w-full bg-white border border-gray-300 mb-6">
          <thead>
            <tr class="bg-indigo-500 text-white">
              <th class="py-2 px-4 border-b">Name</th>
              <th class="py-2 px-4 border-b">Type</th>
              <th class="py-2 px-4 border-b">Seats</th>
              <th class="py-2 px-4 border-b">Pick</th>
              <th class="py-2 px-4 border-b">Drop</th>
              <th class="py-2 px-4 border-b">Departure</th>
              <th class="py-2 px-4 border-b">Arrival</th>
              <th class="py-2 px-4 border-b">Ticket Number</th>
            </tr>
          </thead>
          <tbody>
            <tr class="hover:bg-gray-100">
              <td class="py-2 px-4 border-b text-center">
                <%= train.train.train_name %>
              </td>
              <td class="py-2 px-4 border-b text-center">
                <%= train.train.train_type %>
              </td>
              <td class="py-2 px-4 border-b text-center"><%= train.seats %></td>
              <td class="py-2 px-4 border-b text-center">
                <%= train.starting_station %>
              </td>
              <td class="py-2 px-4 border-b text-center">
                <%= train.ending_station %>
              </td>
              <td class="py-2 px-4 border-b text-center">
                <%= train.departure_time %>
              </td>
              <td class="py-2 px-4 border-b text-center">
                <%= train.arrival_time %>
              </td>
              <td class="py-2 px-4 border-b text-center">
                <%= train.ticket_number %>
              </td>
            </tr>
          </tbody>
        </table>

        <div class="w-full py-1 flex items-end justify-end pt-4">
          <button
            type="submit"
            class="text-white py-2 px-5 text-sm rounded-sm bg-rose-400"
          >
            Cancel Booking
          </button>
        </div>
      </form>
      <% }); %> <% } else { %>
      <div
        class="bg-red-100 shadow-lg rounded-lg p-6 mb-6 border-l-4 border-red-500"
      >
        <div class="flex items-center">
          <!-- Error Icon -->
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-6 w-6 text-red-500 mr-3"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M18.364 5.636a9 9 0 11-12.728 12.728 9 9 0 0112.728-12.728zM12 8v4m0 4h.01"
            />
          </svg>
          <!-- Error Message -->
          <p class="text-red-600 font-medium text-lg">
            No booking details available. Please try again later.
          </p>
        </div>
      </div>

      <% } %>
    </div>
  </body>
</html>
